# Maritime Onboarding Platform - Environment Configuration Template
# Copy this file to .env and fill in your secure values
# NEVER commit .env files with real values to version control

# ⚠️  SECURITY WARNING ⚠️
# NEVER use the example values below in production!
# Generate strong, unique passwords for each service.

# =============================================================================
# DATABASE CONFIGURATION (Required for Docker deployment)
# =============================================================================
DB_NAME=employee_onboarding
DB_USER=postgres
# REQUIRED: Set a strong password (minimum 16 characters, mixed case, numbers, symbols)
# Example generation: openssl rand -base64 32
DB_PASSWORD=your_secure_database_password_here

# Legacy format for compatibility (will be auto-generated from above)
DATABASE_URL=postgresql://postgres:your_secure_database_password_here@localhost:5432/employee_onboarding

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# REQUIRED: JWT secret for token signing (minimum 32 characters)
# Example generation: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_here

# REQUIRED: NextAuth secret (minimum 32 characters)
# Can be the same as JWT_SECRET or different
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Magic link settings
MAGIC_LINK_EXPIRY=3600  # seconds (1 hour)

# =============================================================================
# ADMIN INTERFACE (PgAdmin)
# =============================================================================
PGADMIN_EMAIL=admin@yourdomain.com
# REQUIRED: Strong password for PgAdmin access
PGADMIN_PASSWORD=your_pgadmin_password_here

# =============================================================================
# FILE STORAGE (MinIO)
# =============================================================================
MINIO_ROOT_USER=minioadmin
# REQUIRED: Strong password for MinIO storage access
MINIO_ROOT_PASSWORD=your_minio_password_here

# ========================================
# EMAIL CONFIGURATION (MailerSend)
# ========================================
EMAIL_SERVER=smtp.mailersend.net
EMAIL_PORT=587
EMAIL_TLS=true
EMAIL_USER=your-mailersend-username
EMAIL_PASSWORD=your-mailersend-password

# Company email addresses
HR_EMAIL=hr@yourcompany.com
QHSE_EMAIL=qhse@yourcompany.com

# Email verification settings
EMAIL_VERIFICATION_ENABLED=true
EMAIL_VERIFICATION_TIMEOUT=86400  # seconds (24 hours)

# Email cleanup settings
EMAIL_CLEANUP_ENABLED=true
EMAIL_CLEANUP_INTERVAL_HOURS=24
EMAIL_CLEANUP_BATCH_SIZE=100
EMAIL_CLEANUP_MAX_BATCHES=10

# ========================================
# MULTI-FACTOR AUTHENTICATION (MFA)
# ========================================
MFA_ENABLED=true
MFA_ENFORCEMENT=optional  # optional | required
MFA_BACKUP_CODES=8  # number of backup codes
MFA_ENCRYPTION_KEY=your-mfa-encryption-key-here

# ========================================
# API CONFIGURATION
# ========================================
API_KEY=your-api-key-here
BASE_URL=http://localhost:3000

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000  # 1 minute
RATE_LIMIT_MAX_REQUESTS=100

# ========================================
# DEPLOYMENT & ENVIRONMENT
# ========================================
NODE_ENV=development  # development | test | production

# Vercel deployment
VERCEL_OIDC_TOKEN=your-vercel-token-here

# Cron job secret (for scheduled tasks)
CRON_SECRET=your-cron-secret-here

# ========================================
# TESTING
# ========================================
TEST_EMAIL_DOMAIN=@test.example.com

# ========================================
# NOTES FOR DEVELOPERS
# ========================================
# 1. Generate secure secrets with: openssl rand -base64 32
# 2. Never use the same secrets in production as in development
# 3. For Supabase setup, see: docs/SETUP.md
# 4. For MailerSend setup, create account at: https://www.mailersend.com
# 5. All environment variables must be set for the application to work correctly